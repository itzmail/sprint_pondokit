import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    container: {
        padding: 30,
        flexWrap: 'wrap'
    },
    title: {
        fontSize: 30,
        marginHorizontal: '25%',
        marginTop: 30,
        color: '#b60cd4',
        fontWeight: 'bold'
    },
    subTitle: {
        marginHorizontal: '25%',
        marginVertical: 50
    }
})

export default styles;